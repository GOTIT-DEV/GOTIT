{# Résultats form 1 #}
{% trans_default_domain 'requetes' %}
<div class="table-responsive">
  <table id="result-table" class="table table-condensed table-hover table-striped">
    <thead>
      <tr>
        <th id="col-taxname" data-link-url="{{path('referentieltaxon_show', {id:0})|trim(0)}}">
          {{ "columns.taxon" | trans}}
        </th>
        <th>{{"columns.methode" | trans}}</th>
        <th>{{"columns.dataset" | trans}}</th>
        <th>{{"columns.seqs" | trans}}</th>
        <th>{{"columns.motus" | trans}}</th>
        <th id="col-details" data-link-url="{{ path('detailsModal') }}">
          {{"columns.details" | trans}}
        </th>
      </tr>
    </thead>
    <tbody></tbody>
  </table>
</div>
