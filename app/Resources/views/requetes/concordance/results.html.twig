{# Résultats formulaire 3 #}

{% trans_default_domain 'requetes' %}

<div class="table-responsive-md">
  <table id="result-table" class="table table-condensed table-hover table-striped" style="width:100%">
    <thead
      data-url-lot-materiel="{{path('lotmateriel_show', {id:0})|trim(0)}}"
      data-url-individu="{{path('individu_show', {id:0})|trim(0)}}"
      data-url-sequence="{{path('sequenceassemblee_show', {id:0})|trim(0)}}"
      data-url-ref-taxon="{{path('referentieltaxon_show', {id:0})|trim(0)}}">
      <tr>
        <th></th>
        <th colspan="3">
          {{"entity.lotmateriel.label" | trans}}
        </th>
        <th colspan="4">
          {{"entity.individu.label" | trans}}
        </th>
        <th colspan="3">
          {{"entity.seq.label" | trans}}
        </th>
      </tr>
      <tr>
        <th></th>
        <th>
          {{"entity.lotmateriel.code" | trans}}
        </th>
        <th>{{"columns.taxon" | trans}}</th>
        <th>{{"columns.critere" | trans}}</th>
        <th>
          {{"entity.individu.code.biomol" | trans}}
        </th>
        <th>
          {{"entity.individu.code.trimorpho" | trans}}
        </th>
        <th>{{"columns.taxon" | trans}}</th>
        <th>{{"columns.critere" | trans}}</th>
        <th>
          {{"entity.seq.code" | trans}}
        </th>
        <th>{{"columns.taxon" | trans}}</th>
        <th>{{"columns.critere" | trans}}</th>
      </tr>
    </thead>
    <tbody></tbody>
  </table>
</div>
