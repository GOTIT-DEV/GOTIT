{% extends 'edit.html.twig' %}

{# EDIT : PERSON #}

{% block body %}
    
    {{ parent() }}   

{% endblock %}

{% block scripts %}
{{ parent() }}
{% if exception_message is defined %} <script type="text/javascript"> alert("{{exception_message|raw}}"); </script> {% endif %}

<script type="text/javascript">  
// Form actions
$(document).ready(function(){
        forceUppercase($('#bbees_e3sbundle_personne_nomPersonne'));
        forceUppercase($('#bbees_e3sbundle_personne_nomComplet')); 
        {% if action == 'edit'  %}  
            // General action on form: form lock (default) / user role: app.user ... 
        {% endif %}
        {% if action == 'new'  %} 
            // General action on form: form lock (default) / user role: app.user ...        
        {% endif %}
    });
</script>

{% endblock %}
