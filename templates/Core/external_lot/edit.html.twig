{% extends 'edit.html.twig' %}

{# EDIT : EXTERNAL MATERIAL #}

{% block action %}
	{{parent()}}
	{% if edit_form.vars.data.collecteFk is not null %}
		<a href="{{path('collecte_show', {id: edit_form.vars.data.collecteFk.id})}}" class="btn btn-sm btn-info">
			<i class="fas fa-link"></i>
			{{'button.Back to collecte'|trans}}
		</a>
	{% endif %}
{% endblock %}

{% block body %}
	{{ parent() }}
{% endblock %}

{% block scripts %}
	{{ parent() }}
	{{ encore_entry_script_tags("biomaterial-ext-form") }}
{% endblock %}
