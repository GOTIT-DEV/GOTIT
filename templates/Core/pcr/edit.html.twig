{% extends 'edit.html.twig' %}

{# EDIT : PCR #}

{% block action %}
	{{parent()}}
	{% if edit_form.vars.data.adnFk is not null %}
		<a href="{{path('adn_show', {id:edit_form.vars.data.adnFk.id})}}" class="btn btn-sm btn-info">
			<i class="fas fa-link"></i>
			{{'button.Back to adn'|trans}}
		</a>
	{% endif %}
{% endblock %}

{% block body %}
	{{ parent() }}
{% endblock %}

{% block scripts %}
	{{ parent() }}
	{{encore_entry_script_tags("pcr-form")}}
{% endblock %}
