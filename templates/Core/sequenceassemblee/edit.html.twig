{# EDIT : INTERNAL SEQUENCE #}

{% extends 'edit.html.twig' %}

{% set route = app.request.attributes.get('_route') %}
{% form_theme form_gene_indbiomol 'Form/form-theme.html.twig' %}

{% block body %}

	{{ form(form_gene_indbiomol) }}

		<hr>

		{% if edit_form.vars.action_type != 'new' or
    (geneVocFk|default(null) and specimenFk|default(null)) %}
			{{ parent() }}
		{% endif %}

	{% endblock %}


	{% block scripts %}
		{{ parent() }}
		{{encore_entry_script_tags("sequence-form")}}
	{% endblock %}
